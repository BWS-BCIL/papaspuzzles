name: Disabled GitHub Pages (Vercel in use)

on:
  workflow_dispatch:

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "GitHub Pages deployment disabled. Use Vercel instead."
